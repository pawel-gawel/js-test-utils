import __DESCRIPTION__ from './__ORIGIN_FILENAME__';

describe('__DESCRIPTION__', () => {
  beforeEach(() => {
    
  });

  it('should be true', () => {
    expect(true).to.be.true;
  });

  afterEach(() => {

  });
});
